import Container from "../ui/Container"
import Logo from "../ui/Logo"

const Header = () => {
  return (
    <header>
      <Container>
        <Logo />
      </Container>
    </header>
  )
}

export default Header